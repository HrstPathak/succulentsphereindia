export {
  AUTH_COOKIE_NAME,
  clearAuthCookies,
  createSessionCookie,
  ensureUserProfile,
  getAuthenticatedCustomer,
  requireAuthenticatedUid,
  revokeCurrentSession,
  setSessionCookie,
} from "@/lib/firebase-auth";
