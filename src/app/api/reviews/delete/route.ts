import { NextResponse } from "next/server";
import { clearAuthCookies, getAuthenticatedCustomer } from "@/lib/auth";
import { getFirebaseDb } from "@/lib/firebase-admin";
import { getReviewStats, type ProductReview } from "@/lib/reviews";
export async function POST(request: Request) { try { const session = await getAuthenticatedCustomer(); if (!session.uid) { const r = NextResponse.json({ error: "Please login to delete your review." }, { status: 401 }); clearAuthCookies(r); return r; } const { reviewId, productId } = await request.json(); const ref = getFirebaseDb().collection("reviews").doc(String(reviewId || "")); const snapshot = await ref.get(); if (!snapshot.exists || snapshot.get("userId") !== session.uid) return NextResponse.json({ error: "Review not found." }, { status: 404 }); const id = String(productId || snapshot.get("productId")); await ref.delete(); const reviews = (await getFirebaseDb().collection("reviews").where("productId", "==", id).where("status", "==", "published").get()).docs.map((doc) => ({ id: doc.id, ...doc.data() } as ProductReview)); const stats = getReviewStats(reviews); return NextResponse.json({ ok: true, reviews, reviewCount: stats.reviewCount, rating: stats.averageRating }); } catch (error) { return NextResponse.json({ error: (error as Error).message || "Unable to delete review." }, { status: 500 }); } }
