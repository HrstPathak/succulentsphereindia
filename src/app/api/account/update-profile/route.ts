import { NextResponse } from "next/server";
import { clearAuthCookies, getAuthenticatedCustomer } from "@/lib/auth";
import { fetchCustomerByUid, updateUserProfile } from "@/lib/commerce";
export async function POST(request: Request) { try { const session = await getAuthenticatedCustomer(); if (!session.uid) { const r = NextResponse.json({ error: "Unauthorized." }, { status: 401 }); clearAuthCookies(r); return r; } const { firstName, lastName, phone } = await request.json(); if (!String(firstName || "").trim() || !String(lastName || "").trim()) return NextResponse.json({ error: "First name and last name are required." }, { status: 400 }); await updateUserProfile(session.uid, { firstName: String(firstName).trim(), lastName: String(lastName).trim(), phone: String(phone || "").trim() }); return NextResponse.json({ ok: true, customer: await fetchCustomerByUid(session.uid) }); } catch (error) { return NextResponse.json({ error: (error as Error).message || "Unable to update profile." }, { status: 500 }); } }
